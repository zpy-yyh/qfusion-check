# QFusion 安装前检查脚本 - 依赖要求说明

> 版本: 1.0
> 更新时间: 2026-03-06

## 📋 必需依赖（系统自带）

### 所有脚本都需要
- **Bash** - 系统自带，用于执行脚本
- **SSH/SCP** - 系统自带，用于远程连接
- **基础命令** - ls, grep, awk, sed, cat 等

### 无需额外安装
```bash
✅ 这些都是系统自带的功能
✅ 不需要ISO镜像
✅ 不需要网络连接
✅ 不需要YUM源
```

---

## 🔑 批量部署认证方案

### 方案对比

| 方案 | 需要额外软件 | 优点 | 缺点 | 推荐度 |
|------|-------------|------|------|--------|
| **SSH密钥认证** | ❌ 无 | 无需额外软件<br>更安全<br>生产环境推荐 | 需要预先配置密钥 | ⭐⭐⭐⭐⭐ |
| **expect脚本** | ✅ expect | 支持密码认证<br>配置简单 | 需要安装expect | ⭐⭐⭐⭐ |
| **sshpass** | ✅ sshpass | 简单易用 | 需要epel源<br>很多YUM源没有 | ⭐⭐⭐ |

---

## 🎯 推荐方案：SSH密钥认证

### ✅ 优势
- **无需安装任何额外软件**
- **使用系统自带SSH功能**
- **更安全可靠**
- **适合生产环境**
- **不需要ISO或网络**

### 📝 配置步骤

#### 步骤1: 生成SSH密钥（管理节点）

```bash
ssh-keygen -t rsa -b 2048 -N "" -f ~/.ssh/id_rsa
```

#### 步骤2: 配置免密登录到各节点

**方法A：使用自动配置脚本**

```bash
bash /root/zpy/setup_ssh_keys.sh
```

**方法B：手动配置**

```bash
# 对每个节点执行
ssh-copy-id root@10.10.156.97
ssh-copy-id root@10.10.156.98
# ... 其他节点
```

**方法C：使用expect脚本（如果节点配置中有密码）**

```bash
# setup_ssh_keys.sh会自动使用expect
bash /root/zpy/setup_ssh_keys.sh
```

#### 步骤3: 更新节点配置文件

```bash
cat > /root/zpy/qfusion_package/nodes.conf << EOF
# 使用SSH密钥认证
10.10.156.97 rds001 master root /root/.ssh/id_rsa
10.10.156.98 rds002 master root /root/.ssh/id_rsa
10.10.156.99 rds003 master root /root/.ssh/id_rsa
10.10.156.87 rds004 worker root /root/.ssh/id_rsa
10.10.156.88 rds005 worker root /root/.ssh/id_rsa
10.10.156.89 rds006 worker root /root/.ssh/id_rsa
