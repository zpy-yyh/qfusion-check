# QFusion 批量远程部署测试指南

## 测试环境状态

### 当前环境信息
- 本机IP: 10.10.156.54
- SSH密钥: 未生成
- sshpass: 未安装
- 节点配置: 未配置

## 测试方案

### 方案A: 真实环境测试（推荐用于生产）

如果您有三台实际的服务器，可以按照以下步骤配置：

#### 步骤1: 配置节点列表
```bash
cat > /root/zpy/qfusion_package/nodes.conf << EOF
# 节点1
192.168.1.10 node1 master root password123
# 节点2
192.168.1.11 node2 worker root password456
# 节点3
192.168.1.12 node3 worker root password789
EOF
```

#### 步骤2: 安装sshpass（密码认证需要）
```bash
yum install -y sshpass
```

#### 步骤3: 运行批量部署
```bash
cd /root/zpy/qfusion_package
bash qfusion.sh --remote-init
```

#### 步骤4: 或使用交互式���单
```bash
bash qfusion.sh
# 选择 "2. 批量节点管理"
# 选择 "2. 批量检查和初始化"
```

---

### 方案B: 本地模拟测试（推荐用于开发测试）

#### 步骤1: 创建本地测试节点
```bash
# 使用localhost创建三个测试节点
cat > /root/zpy/qfusion_package/nodes.conf << EOF
# 本地测试节点（使用本机模拟三台设备）
127.0.0.1 localhost-test-1 master root root
127.0.0.1 localhost-test-2 worker root root
127.0.0.1 localhost-test-3 worker root root
EOF
```

#### 步骤2: 生成本机SSH密钥
```bash
ssh-keygen -t rsa -b 2048 -N "" -f ~/.ssh/id_rsa
# 配置本机免密登录
cat ~/.ssh/id_rsa.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

#### 步骤3: 测试SSH连接
```bash
# 测试本机SSH
ssh -o StrictHostKeyChecking=no root@localhost "hostname"
```

#### 步骤4: 运行批量检查
```bash
cd /root/zpy/qfusion_package
bash qfusion.sh --check
```

---

### 方案C: Docker容器测试（推荐用于隔离测试）

#### 步骤1: 创建Docker测试环境
```bash
# 拉取CentOS镜像
docker pull centos:7

# 创建三个容器
docker run -d --name qfusion-node1 --hostname node1 centos:7 /usr/sbin/init
docker run -d --name qfusion-node2 --hostname node2 centos:7 /usr/sbin/init
docker run -d --name qfusion-node3 --hostname node3 centos:7 /usr/sbin/init

# 获取容器IP
docker inspect qfusion-node1 | grep IPAddress
docker inspect qfusion-node2 | grep IPAddress
docker inspect qfusion-node3 | grep IPAddress
```

#### 步骤2: 配置容器密码
```bash
# 设置root密码
docker exec qfusion-node1 passwd -d root
docker exec qfusion-node2 passwd -d root
docker exec qfusion-node3 passwd -d root
```

#### 步骤3: 配置节点列表
```bash
cat > /root/zpy/qfusion_package/nodes.conf << EOF
172.17.0.2 node1 master root root
172.17.0.3 node2 worker root root
172.17.0.4 node3 worker root root
EOF
```

---

## 验证批量部署功能

### 1. 检查批量部署函数
```bash
# 查看批量管理菜单
grep -A 30 "batch_management_menu()" qfusion_package/qfusion.sh
```

### 2. 检查SSH连接脚本
```bash
# 查看expect脚本
ls -la /root/ssh_*.exp 2>/dev/null || echo "expect脚本将在首次运行时创建"
```

### 3. 检查远程初始化功能
```bash
# 查看远程初始化参数处理
grep -A 20 "remote-init" qfusion_package/qfusion.sh
```

---

## 测试检查清单

### 环境准备
- [ ] 节点配置文件已创建
- [ ] SSH密钥已生成（密钥认证）
- [ ] sshpass已安装（密码认证）
- [ ] 网络连通性已验证

### 功能测试
- [ ] 单节点检查功能
- [ ] 批量节点检查功能
- [ ] SSH免密登录配置
- [ ] 远程初始化功能
- [ ] 节点列表管理功能

### 结果验证
- [ ] 所有节点检查结果正确
- [ ] 错误节点能够正确识别
- [ ] 日志输出清晰可读
- [ ] 失败重试机制正常

---

## 常见问题

### Q1: SSH连接失败
**原因**: 目标节点SSH服务未启动或防火墙阻止
**解决**:
```bash
# 检查SSH服务
systemctl status sshd

# 检查防火墙
firewall-cmd --list-all
# 如果需要，开放SSH端口
firewall-cmd --add-service=ssh --permanent
firewall-cmd --reload
```

### Q2: 密码认证失败
**原因**: sshpass未安装或密码错误
**解决**:
```bash
# 安装sshpass
yum install -y sshpass

# 验证密码
sshpass -p 'password' ssh root@IP 'hostname'
```

### Q3: 节点配置格式错误
**原因**: nodes.conf格式不正确
**解决**: 确保每行格式为 `IP 主机名 角色 用户 认证信息`

---

## 下一步

请选择您想要使用的测试方案：
1. 方案A - 提供实际的服务器信息
2. 方案B - 使用本地模拟测试
3. 方案C - 使用Docker容器测试
4. 其他 - 自定义测试方案
