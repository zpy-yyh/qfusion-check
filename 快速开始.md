# QFusion安装前检查脚本 - 快速开始

> 版本: 1.0
> 打包时间: 2026-03-06 11:45:33

## 📦 tar包信息

**文件名**: qfusion_precheck_script_20260306_114533.tar.gz
**大小**: 60KB
**位置**: /root/zpy/

## 📋 包含文件

### 核心文件
- qfusion.sh - 主检查脚本
- install.sh - 安装脚本
- README.md - 详细使用说明

### 批量检查脚本
- batch_check_only_expect.sh - expect认证版本（推荐）
- batch_check_only.sh - SSH密钥认证版本
- setup_ssh_keys.sh - SSH密钥配置辅助脚本

### 配置文件
- nodes.conf - 节点配置文件（已包含您的6台服务器配置）
- nodes.conf.example - 节点配置模板

### 文档
- QFusion手动操作指南.md - 3个手动操作项详细说明
- QFusion安装前检查项-未实现项分析.md - 未实现项分析
- SSH认证方案说明.md - 认证方案对比
- 依赖要求说明.md - 依赖和配置说明
- 其他测试报告文档

## 🚀 部署步骤

### 方式1: 手动分步执行（推荐新手）

#### 步骤1: 上传tar包到目标服务器
```bash
scp /root/zpy/qfusion_precheck_script_20260306_114533.tar.gz root@目标服务器IP:/tmp/
```

#### 步骤2: 解压tar包
```bash
ssh root@目标服务器IP "cd /tmp && tar -xzf qfusion_precheck_script_20260306_114533.tar.gz"
```

#### 步骤3: 安装脚本
```bash
ssh root@目标服务器IP "cd /tmp/qfusion_package_2026-03-06_11:45:33/qfusion_package && bash install.sh"
```

#### 步骤4: 查看脚本帮助
```bash
ssh root@目标服务器IP "qfusion --help"
```

#### 步骤5: 本地单节点测试
```bash
ssh root@目标服务器IP "qfusion --check"
```

#### 步骤6: 查看节点配置
```bash
ssh root@目标服务器IP "cat /tmp/qfusion_package_2026-03-06_11:45:33/config/nodes.conf"
```

#### 步骤7: 批量检查
```bash
ssh root@目标服务器IP "cd /tmp/qfusion_package_2026-03-06_11:45:33/scripts && bash batch_check_only_expect.sh"
```

### 方式2: 一条命令完成（推荐熟练用户）

```bash
scp /root/zpy/qfusion_precheck_script_20260306_114533.tar.gz root@目标服务器IP:/tmp/ && \
ssh root@目标服务器IP "cd /tmp && tar -xzf qfusion_precheck_script_20260306_114533.tar.gz && \
cd qfusion_package_2026-03-06_11:45:33/qfusion_package && bash install.sh && \
cd ../scripts && bash batch_check_only_expect.sh"
```

### 方式3: 分步骤批量部署（多台服务器）

#### 1. 将tar包传到第一台服务器
```bash
scp /root/zpy/qfusion_precheck_script_20260306_114533.tar.gz root@node1:/tmp/
```

#### 2. 在第一台服务器执行
```bash
ssh root@node1 "cd /tmp && tar -xzf qfusion_precheck_script_20260306_114533.tar.gz && \
cd qfusion_package_2026-03-06_11:45:33/qfusion_package && bash install.sh"
```

#### 3. 在第一台服务器分发到其他节点
```bash
# 使用scp或rsync分发到其他节点
ssh root@node1 "cd /tmp && scp qfusion_precheck_script_20260306_114533.tar.gz node2:/tmp/"
ssh root@node1 "cd /tmp && scp qfusion_precheck_script_20260306_114533.tar.gz node3:/tmp/"
# ... 其他节点
```

#### 4. 在每台服务器重复安装和检查
```bash
ssh root@node2 "cd /tmp && tar -xzf qfusion_precheck_script_20260306_114533.tar.gz && \
cd qfusion_package_2026-03-06_11:45:33/qfusion_package && bash install.sh"

# 对每台服务器配置nodes.conf并执行检查
```

## 📝 节点配置文件说明

### 配置格式
```conf
IP地址 主机名 角色 用户 认证信息
```

### 示例配置（您的6台服务器）
```conf
10.10.156.97 rds001 master root Cljslrl0620!
10.10.156.98 rds002 master root Cljslrl0620!
10.10.156.99 rds003 master root Cljslrl0620!
10.10.156.87 rds004 worker root Cljslrl0620!
10.10.156.88 rds005 worker root Cljslrl0620!
10.10.156.89 rds006 worker root Cljslrl0620!
```

### 修改节点配置
```bash
# 方法1: 在目标服务器直接编辑
ssh root@node1 "vim /tmp/qfusion_package_2026-03-06_11:45:33/config/nodes.conf"

# 方法2: 修改本地后重新打包
vim /root/zpy/qfusion_package/nodes.conf
# 然后重新运行打包脚本：bash /root/zpy/package_qfusion.sh
```

### 使用SSH密钥认证
```bash
# 1. 生成SSH密钥（在管理节点）
ssh-keygen -t rsa -b 2048 -N "" -f ~/.ssh/id_rsa

# 2. 配置免密登录（需要expect或sshpass）
bash /tmp/qfusion_package_2026-03-06_11:45:33/scripts/setup_ssh_keys.sh

# 3. 更新nodes.conf使用密钥认证
# 将: 10.10.156.97 rds001 master root Cljslrl0620!
# 改为: 10.10.156.97 rds001 master root /root/.ssh/id_rsa
```

## 🔑 认证方式选择

### expect认证（推荐用于测试）
- ✅ 配置简单
- ✅ 无需SSH密钥
- ✅ 直接使用密码
- ⚠️ 配置文件包含明文密码

### SSH密钥认证（推荐生产环境）
- ✅ 更安全
- ✅ 配置文件无明文密码
- ✅ 一次配置，长期使用
- ⚠️ 需要预先配置免密登录

## ⚠️ 安全注意事项

### 密码安全
1. tar包包含nodes.conf，其中有明文密码
2. 测试完成后建议删除或清空密码
3. 不要将tar包发送给无关人员
4. 使用后及时删除tar包

### 清空密码方法
```bash
# 在目标服务器执行
sed -i 's/Cljslrl0620!/xxxxxx/g' /tmp/qfusion_package_*/config/nodes.conf

# 或直接删除配置文件
rm /tmp/qfusion_package_*/config/nodes.conf

# 或删除整个tar包
rm /tmp/qfusion_precheck_script_*.tar.gz
```

### 权限设置
```bash
# 设置配置文件权限
chmod 600 /tmp/qfusion_package_*/config/nodes.conf

# 删除临时文件
rm -rf /tmp/qfusion_package_2026-03-06_11:45:33
```

## 📊 检查结果查看

### 检查日志位置
```bash
# 结果目录
ls -lh /root/zpy/check_results_*/

# 查看汇总报告
cat /root/zpy/check_results_*/summary_report.txt

# 查看单个节点日志
cat /root/zpy/check_results_*/10.10.156.97/check_result.log
```

### 结果导出
```bash
# 复制结果到本地
scp -r root@node1:/root/zpy/check_results_* /root/

# 或打包下载
ssh root@node1 "cd /root/zpy && tar -czf check_results.tar.gz check_results_*"
scp root@node1:/root/zpy/check_results.tar.gz /root/
```

## 🐛 故障排查

### 问题1: 无法上传tar包
```bash
# 检查网络连通性
ping 目标服务器IP

# 检查SSH连接
ssh root@目标服务器IP "echo 'OK'"

# 检查磁盘空间
ssh root@目标服务器IP "df -h /tmp"
```

### 问题2: 无法解压
```bash
# 检查tar包完整性
ssh root@目标服务器IP "cd /tmp && tar -tzf qfusion_precheck_script_20260306_114533.tar.gz | head -5"

# 重新下载
# 如果tar包损坏，重新从本地上传
```

### 问题3: 脚本执行失败
```bash
# 查看脚本权限
ssh root@目标服务器IP "ls -l /tmp/qfusion_package_*/qfusion.sh"

# 查看详细错误
ssh root@目标服务器IP "bash /tmp/qfusion_package_*/qfusion.sh --check 2>&1 | tail -50"
```

### 问题4: SSH连接失败
```bash
# 检查密码
echo "手动尝试: ssh root@目标服务器IP"

# 检查SSH服务
ssh root@目标服务器IP "systemctl status sshd"

# 查看防火墙
ssh root@目标服务器IP "firewall-cmd --list-all"
```

## 📞 获取帮助

### 查看脚本帮助
```bash
qfusion --help
```

### 查看详细文档
```bash
# 在解压后的目录
cat /tmp/qfusion_package_*/README.md

# 手动操作指南
cat /tmp/qfusion_package_*/docs/QFusion手动操作指南.md
```

## 🎉 开始使用

一切准备就绪！按照上述步骤开始使用QFusion安装前检查脚本。

**记住**: tar包位置在 /root/zpy/qfusion_precheck_script_20260306_114533.tar.gz
