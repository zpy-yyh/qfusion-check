# QFusion 3master+3node 集群检查配置说明

## 📋 您需要提供的节点配置格式

请按照以下格式提供您的6台服务器信息：

### 配置格式

```
IP地址 主机名 角色 用户名 认证信息
```

### 字段说明

| 字段 | 说明 | 示例 |
|------|------|------|
| IP地址 | 服务器的IP地址 | 192.168.1.10 |
| 主机名 | 服务器的实际主机名 | qfusion-master-1 |
| 角色 | master 或 worker | master |
| 用户名 | SSH登录用户名 | root |
| 认证信息 | 密码或SSH密钥路径 | MyPassword123 或 /root/.ssh/id_rsa |

### 完整配置示例

```conf
# Master节点（3台）
192.168.1.10 qfusion-master-1 master root your_password_here
192.168.1.11 qfusion-master-2 master root your_password_here
192.168.1.12 qfusion-master-3 master root your_password_here

# Worker节点（3台）
192.168.1.13 qfusion-worker-1 worker root your_password_here
192.168.1.14 qfusion-worker-2 worker root your_password_here
192.168.1.15 qfusion-worker-3 worker root your_password_here
```

### 使用SSH密钥的示例

```conf
# Master节点（使用SSH密钥）
192.168.1.10 qfusion-master-1 master root /root/.ssh/id_rsa
192.168.1.11 qfusion-master-2 master root /root/.ssh/id_rsa
192.168.1.12 qfusion-master-3 master root /root/.ssh/id_rsa

# Worker节点（使用SSH密钥）
192.168.1.13 qfusion-worker-1 worker root /root/.ssh/id_rsa
192.168.1.14 qfusion-worker-2 worker root /root/.ssh/id_rsa
192.168.1.15 qfusion-worker-3 worker root /root/.ssh/id_rsa
```

### 混合认证示例

```conf
# 有些用密码，有些用密钥
192.168.1.10 qfusion-master-1 master root MyPassword123
192.168.1.11 qfusion-master-2 master root /root/.ssh/id_rsa
192.168.1.12 qfusion-master-3 master root MyPassword456
192.168.1.13 qfusion-worker-1 worker root /root/.ssh/id_rsa
192.168.1.14 qfusion-worker-2 worker root MyPassword789
192.168.1.15 qfusion-worker-3 worker root /root/.ssh/id_rsa
```

## ⚙️ 配置说明

### 1. 密码认证
- 直接填写密码（明文）
- 需要安装sshpass工具
- 安装命令: `yum install -y sshpass`

### 2. SSH密钥认证（推荐）
- 填写私钥文件路径
- 更安全，推荐使用
- 需要提前配置免密登录

### 3. 混合认证
- 可以部分节点用密码，部分用密钥
- 灵活配置

## 🔒 安全说明

### 密码文件安全
配置文件包含明文密码，建议：

1. **设置文件权限**
```bash
chmod 600 /root/zpy/qfusion_package/nodes.conf
```

2. **检查完成后删除密码**
```bash
# 检查完成后，删除或清空密码
sed -i 's/your_password/xxxxxx/g' /root/zpy/qfusion_package/nodes.conf
```

3. **使用密钥认证（推荐）**
```bash
# 生成SSH密钥
ssh-keygen -t rsa -b 2048 -f ~/.ssh/id_rsa -N ""

# 配置免密登录到各节点
ssh-copy-id root@node_ip
```

## 📝 配置步骤

### 步骤1: 创建节点配置文件

```bash
cat > /root/zpy/qfusion_package/nodes.conf << 'EOF'
# 将下面的示例替换为您的实际配置
192.168.1.10 qfusion-master-1 master root your_password
192.168.1.11 qfusion-master-2 master root your_password
192.168.1.12 qfusion-master-3 master root your_password
192.168.1.13 qfusion-worker-1 worker root your_password
192.168.1.14 qfusion-worker-2 worker root your_password
192.168.1.15 qfusion-worker-3 worker root your_password
EOF
```

### 步骤2: 设置文件权限

```bash
chmod 600 /root/zpy/qfusion_package/nodes.conf
```

### 步骤3: 验证配置文件

```bash
cat /root/zpy/qfusion_package/nodes.conf
```

### 步骤4: 运行检查

```bash
bash /root/zpy/batch_check_only.sh
```

## 🎯 执行模式说明

此脚本使用**只读检查模式**，会：
- ✅ 检查系统配置
- ✅ 验证安装前检查项
- ✅ 生成详细报告
- ❌ **不会修改任何配置**
- ❌ **不会执行任何修复操作**

## 📊 检查结果

检查完成后会生成：

1. **详细日志** - 每个节点的检查日志
2. **汇总报告** - 所有节点的统计信息
3. **状态文件** - 每个节点的检查状态

结果保存在: `/root/zpy/check_results_YYYYMMDD_HHMMSS/`

## 📋 示例配置模板（复制并修改）

```bash
# ============================================
# 请修改以下配置为您的实际信息
# ============================================

# Master节点 1
192.168.1.___ qfusion-master-1 master root ____

# Master节点 2
192.168.1.___ qfusion-master-2 master root ____

# Master节点 3
192.168.1.___ qfusion-master-3 master root ____

# Worker节点 1
192.168.1.___ qfusion-worker-1 worker root ____

# Worker节点 2
192.168.1.___ qfusion-worker-2 worker root ____

# Worker节点 3
192.168.1.___ qfusion-worker-3 worker root ____

# ============================================
# 修改说明:
# 1. 将 ___ 替换为实际IP或密码
# 2. 确认主机名是否正确
# 3. 确认角色是否正确（master/worker）
# ============================================
```

## 🚀 提供配置后

请直接提供您的节点配置，格式如下：

```
192.168.1.10 master-1 master root password123
192.168.1.11 master-2 master root password456
192.168.1.12 master-3 master root password789
192.168.1.13 worker-1 worker root password123
192.168.1.14 worker-2 worker root password456
192.168.1.15 worker-3 worker root password789
```

我会帮您：
1. 创建配置文件
2. 验证配置格式
3. 运行批量检查
4. 生成详细报告
