# QFusion检查脚本修改说明

## 📅 修改时间
2026-03-25

## 🎯 修改目的
将安装前和安装后检查项分离，提供更灵活的检查选项。

---

## 📋 修改内容

### 1. 新增函数

#### 1.1 安装前检查函数 - `run_pre_install_checks()`
- **功能**: 执行25项安装前环境检查
- **检查项**:
  - 系统基础检查（YUM源、内核版本）
  - 网络配置检查（时间、网卡、DNS、网络服务）
  - 安全配置检查（SELinux、iptables）
  - 存储配置检查（Swap、I/O调度、lsblk-fstab、/tmp）
  - 系统服务检查（auditd）
  - 硬件特性检查（CPU AVX、kdump、crashkernel）
  - 软件依赖检查（软件包、容器软件）
  - 其他检查（主机名、virbr0、资源限制、nproc、IPv6、LVM挂载、bpf_jit、RAID）

#### 1.2 安装后检查函数 - `run_post_install_checks()`
- **功能**: 执行10项安装后环境检查
- **前置检查**: 自动检测QFusion是否已安装，未安装则跳过
- **检查项**:
  1. Pod状态检查
  2. 节点状态检查
  3. 容器运行时检查（Containerd/Docker）
  4. Kubelet状态检查
  5. etcd状态检查
  6. CNI网络插件检查
  7. DNS服务检查（CoreDNS）
  8. 存储类检查（StorageClass）
  9. API服务检查（Kubernetes API）
  10. 告警监控检查（Prometheus/Grafana）

#### 1.3 总检查函数 - `run_all_checks()`
- **功能**: 依次执行安装前和安装后检查
- **逻辑**:
  1. 执行安装前检查（25项）
  2. 检测QFusion是否已安装
  3. 如果已安装，执行安装后检查（10项）
  4. 如果未安装，跳过安装后检查

### 2. 新增安装后检查函数

| 函数名 | 功能 | 说明 |
|--------|------|------|
| `check_post_install_pods()` | Pod状态检查 | 检查所有Pod是否Running |
| `check_post_install_nodes()` | 节点状态检查 | 检查所有节点是否Ready |
| `check_post_install_container_runtime()` | 容器运行时检查 | 检查Containerd/Docker服务 |
| `check_post_install_kubelet()` | Kubelet状态检查 | 检查Kubelet服务状态 |
| `check_post_install_etcd()` | etcd状态检查 | 检查etcd服务健康度 |
| `check_post_install_cni()` | CNI网络插件检查 | 检查CNI插件Pod状态 |
| `check_post_install_dns_service()` | DNS服务检查 | 检查CoreDNS Pod状态 |
| `check_post_install_storage_class()` | 存储类检查 | 检查StorageClass配置 |
| `check_post_install_api_service()` | API服务检查 | 检查Kubernetes API连通性 |
| `check_post_install_monitoring()` | 告警监控检查 | 检查Prometheus/Grafana状态 |

### 3. 菜单更新

#### 3.1 单节点操作菜单
```
原菜单:
1. 执行完整检查和修复
2. 配置本地YUM源
3. 只检查不修复
4. 安装sshpass（批量管理需要）
5. 显示上次检查结果
0. 返回主菜单

新菜单:
1. 总检查项（安装前+安装后）
2. 安装前检查项（25项）
3. 安装后检查项（10项）
4. 配置本地YUM源
5. 只检查不修复
6. 安装sshpass（批量管理需要）
7. 显示上次检查结果
0. 返回主菜单
```

#### 3.2 主菜单 - 快速检查选项
```
原逻辑: 执行run_all_checks()
新逻辑:
  1. 总检查项（安装前+安装后）
  2. 安装前检查项（25项）
  3. 安装后检查项（10项）
```

### 4. 命令行参数更新

| 参数 | 功能 | 说明 |
|------|------|------|
| `--check` | 总检查（安装前+安装后） | 原参数保留 |
| `--pre-install` | 只检查安装前（25项） | 新增 |
| `--post-install` | 只检查安装后（10项） | 新增 |
| `--remote-init` | 远程初始化模式 | 原参数保留 |
| `--help` | 显示帮助 | 更新了说明 |

---

## 📖 使用方法

### 方法1: 交互式菜单

```bash
# 启动脚本
bash /root/zpy/qfusion_package/qfusion.sh

# 选择1 - 单节点操作（本机）
# 然后选择:
#   1. 总检查项（安装前+安装后）
#   2. 安装前检查项（25项）
#   3. 安装后检查项（10项）
#   5. 只检查不修复（会进一步选择检查类型）
```

### 方法2: 命令行参数

```bash
# 总检查（安装前+安装后）
bash /root/zpy/qfusion_package/qfusion.sh --check

# 只检查安装前（25项）
bash /root/zpy/qfusion_package/qfusion.sh --pre-install

# 只检查安装后（10项）
bash /root/zpy/qfusion_package/qfusion.sh --post-install

# 查看帮助
bash /root/zpy/qfusion_package/qfusion.sh --help
```

### 方法3: 批量远程检查

```bash
# 使用expect脚本批量检查
# 修改batch_check_only_expect.sh，添加检查类型选择

# 或使用命令行参数
expect -c "
spawn ssh root@node \"bash /tmp/qfusion.sh --pre-install\"
...
"
```

---

## ✅ 测试结果

### 测试1: 帮助信息
```bash
$ bash qfusion.sh --help

用法: qfusion.sh [选项]

选项:
  --remote-init    远程初始化模式
  --check          总检查（安装前+安装后）
  --pre-install     只检查安装前（25项）
  --post-install    只检查安装后（10项）
  --help           显示帮助
```
**结果**: ✅ 正常

### 测试2: 安装前检查
```bash
$ bash qfusion.sh --pre-install

[*] ==========================================
        安装前环境检查（25项）
[*] ==========================================

[INFO] 开始执行安装前检查项...

[*] 检查YUM源配置...
✓ YUM源: 配置正常
...
```
**结果**: ✅ 正常

### 测试3: 安装后检查
```bash
$ bash qfusion.sh --post-install

[*] ==========================================
        安装后环境检查（10项）
[*] ==========================================

[INFO] 开始执行安装后检查项...

[WARN] 未检测到QFusion安装，跳过安装后检查
[INFO] 安装后检查需要在QFusion安装后执行
```
**结果**: ✅ 正常（未安装QFusion时正确跳过）

### 测试4: 总检查
```bash
$ bash qfusion.sh --check

[*] ==========================================
        总检查（安装前+安装后）
[*] ==========================================

[*] ==========================================
        安装前环境检查（25项）
...
（执行完安装前检查）

[INFO] 未检测到QFusion安装，跳过安装后检查
[INFO] 完成安装前检查（25项）
```
**结果**: ✅ 正常

---

## 🎨 界面优化

### 安装前检查界面
```
==========================================
        安装前环境检查（25项）
==========================================

[INFO] 开始执行安装前检查项...

[*] 检查YUM源配置...
✓ YUM源: 配置正常
...

==========================================
           检查汇总
==========================================
```

### 安装后检查界面
```
==========================================
        安装后环境检查（10项）
==========================================

[INFO] 开始执行安装后检查项...

[*] 检查1/10: Pod状态检查...
✓ Pod状态: 所有Pod正常运行

[*] 检查2/10: 节点状态检查...
✓ 节点状态: 所有节点正常（共 3 个）
...
```

---

## 📂 文件备份

```bash
# 原脚本备份
/root/zpy/qfusion_package/qfusion.sh.backup_20260325_174500

# 修改后的脚本
/root/zpy/qfusion_package/qfusion.sh
```

---

## 🔧 修复原代码问题

### 问题1: run_all_checks函数内容重复
- **位置**: 3876-3924行
- **问题**: 检查项代码重复
- **修��**: 已清理重复代码，重新组织函数结构

### 问题2: DNS默认配置
- **位置**: DNS检查函数
- **原配置**: 114.114.114.114（不容易ping通）
- **新配置**: 223.5.5.5（阿里DNS，更容易ping通）
- **状态**: ✅ 已修复

---

## 🎉 修改总结

| 项目 | 修改前 | 修改后 |
|------|---------|---------|
| 检查选项 | 只有完整检查 | 总检查、安装前、安装后 |
| 菜单选项 | 5个选项 | 7个选项 |
| 命令行参数 | 3个参数 | 5个参数 |
| 安装后检查 | 不支持 | 新增10项检查 |
| 智能跳过 | 不支持 | 自动检测QFusion安装状态 |
| 用户体验 | 混合检查 | 灵活选择检查类型 |

---

## 🚀 后续优化建议

1. **批量检查脚本更新**: 修改batch_check_only_expect.sh，支持选择检查类型
2. **安装后检查增强**: 添加更详细的性能指标检查
3. **检查报告优化**: 生成HTML格式的检查报告
4. **自动修复**: 为安装后检查项提供自动修复功能
5. **历史记录**: 保存检查历史，支持趋势分析

---

**文档生成时间**: 2026-03-25 17:50
**脚本版本**: v2.0
**修改人**: Claude Code Assistant
